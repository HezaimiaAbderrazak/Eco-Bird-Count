# Eco-Bird-Count – local installation requirements
# Run:  pip install -r requirements.txt
#
# NOTE: Use opencv-python (not headless) locally so the webcam/preview
#       window works. The Replit environment uses the headless build.

ultralytics>=8.3.0
opencv-python>=4.10.0
requests>=2.32.0
numpy>=1.24.0
Pillow>=10.0.0
google-generativeai>=0.8.0
